# macOS / Windows junk
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp

# Local backups of replaced assets
_old_*.bak
*.bak

# Reference-only folder from the original Claude Design export
# (contains business strategy doc + duplicate assets). Not imported by the app.
uploads/

# Duplicate entry HTML kept around for local dev ergonomics
# (index.html is the canonical one the server loads)
Sandbox Pitch & Putt App.html

# Node (for future port)
node_modules/
dist/
build/

# Env / secrets — NEVER commit. Use .env.example as the template.
.env
.env.local
.env.development.local
.env.production.local
.env.*.local

# Next.js (for future port)
.next/
out/
next-env.d.ts

# Capacitor / native build output (for app-store pathway)
ios/App/Pods/
ios/App/build/
ios/DerivedData/
android/.gradle/
android/build/
android/app/build/
android/local.properties
*.ipa
*.aab
*.apk

# Signing / certificates — store in 1Password/Keychain, not git
*.keystore
*.jks
*.p12
*.mobileprovision
*.cer

# Python http.server
__pycache__/
*.pyc
